general:
  buildTool: 'mta'
  gitSshKeyCredentialsId: 'Sbasu'
  productiveBranch: 'master'
  verbose: true
  script: this

  step:
    
      buildExecute:
        buildTool: 'mta'
        npmInstall: true
   
